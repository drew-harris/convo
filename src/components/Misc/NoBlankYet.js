import React from "react";

const style = {};
const NoBlankYet = ({ message }) => {
  return (
    <div className="noblankyet" style={style}>
      {message}
    </div>
  );
};

export { NoBlankYet };
