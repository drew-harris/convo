import React from "react";
import { auth } from "../../firebase/firebase";
import { Main, Header, Anchor, Box, Button } from "grommet";
import { useHistory } from "react-router-dom";

const Groups = () => {
  const history = useHistory();
  return <div>Groups View</div>;
};

export { Groups };
